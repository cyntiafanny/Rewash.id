export class Item {
    KEY: string;
    NAME: string;
    PRICE: number;
    WEIGHT: number;
    QTY: number;
    constructor(KEY: string, NAME: string, PRICE: number, WEIGHT: number, QTY: number) {
    }
}
